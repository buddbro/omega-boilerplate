function applyExtraSetup(sequelize) {
  // TODO: Uncomment when we actually have character model
  // const { user, character } = sequelize.models

  // user.hasOne(character)
  // character.belongsTo(user)
}

module.exports = { applyExtraSetup }